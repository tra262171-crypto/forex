# Skill: PLACEHOLDER

Purpose: Describe the skill responsibility, inputs, outputs, and constraints.

Standard Input: `market_state`
Standard Output: JSON with `decision`, `confidence`, `evidence`, `metadata`.

Constraints: Follow Single Responsibility principle. Do not perform unrelated tasks.
